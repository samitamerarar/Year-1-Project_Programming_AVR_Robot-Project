/*if(!stop)
	{
		uint16_t distance = captDistance.calculerDistanceMm();
		
		if(distance >= 255)
			distance = 255;
			
		prendreMesures(distance);
	}
	else
	{
		moteur.arreter();
		uart_send(analyserMesures());
	}
	
	if(stop)
		moteur.arreter();*/
